package com.actividad2.libros_y_autores.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/v1/autores")
public class AutorController {
    // Aquí puedes agregar los métodos para manejar las solicitudes HTTP relacionadas con los autores
    // Por ejemplo, un método para obtener todos los autores, otro para crear un nuevo autor, etc.
}
