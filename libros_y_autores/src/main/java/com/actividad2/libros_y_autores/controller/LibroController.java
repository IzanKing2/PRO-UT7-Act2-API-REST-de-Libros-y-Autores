package com.actividad2.libros_y_autores.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/v1/libros")
public class LibroController {
    // Aquí puedes agregar los métodos para manejar las solicitudes HTTP relacionadas con los libros
    // Por ejemplo, un método para obtener todos los libros, otro para crear un nuevo libro, etc.
}